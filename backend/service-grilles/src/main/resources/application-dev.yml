spring:
  datasource:
    url: ${DB_GRILLES_URL:jdbc:postgresql://localhost:5432/rations_grilles}
    username: ${DB_GRILLES_USERNAME:rations_grilles}
    password: ${DB_GRILLES_PASSWORD:changeme-in-development}
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false

logging:
  level:
    root: INFO
    cm.afrilandfirstbank.rations.grilles: DEBUG
