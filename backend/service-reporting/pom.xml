<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>cm.afrilandfirstbank.rations</groupId>
        <artifactId>rations-backend</artifactId>
        <version>0.1.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>service-reporting</artifactId>
    <packaging>jar</packaging>

    <name>service-reporting</name>
    <description>Reporting - suivi, historique, exports PDF et Excel (lecture via les API des autres services, pas de base propre)</description>

    <dependencies>
        <!-- Publication des evenements d'audit sur rations.audit.evenement.
             Bibliotheque partagee par les six services metier (decision Sprint
             1.3) : le producteur, avec ses cinq couches de protection, vit a un
             seul endroit. N'est PAS une dependance vers le service Audit, qui
             reste joignable uniquement par le topic. Ne jamais reecrire un
             producteur local ni surcharger max.block.ms (CLAUDE.md section 15).

             Ici, elle sert d'abord l'obligation CT-04 : ce service consomme
             GET /identite/moi et doit publier ACCES_REFUSE sur verdict negatif
             comme sur indisponibilite du service Identite. -->
        <dependency>
            <groupId>cm.afrilandfirstbank.rations</groupId>
            <artifactId>rations-audit-commun</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        </dependency>
        <dependency>
            <groupId>com.itextpdf</groupId>
            <artifactId>kernel</artifactId>
        </dependency>
        <dependency>
            <groupId>com.itextpdf</groupId>
            <artifactId>layout</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi-ooxml</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Spring Boot 4 : la tranche @WebMvcTest vit dans son propre starter.
             Requise par ReportingControllerIT (Sprint 6.2), qui traverse la
             chaine jeton -> securite -> controleur. Memes dependances qu'ont
             ajoutees service-grilles (Sprint 2.2), service-saisie (Sprint 3.3)
             et service-workflow (Sprint 4.1). -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Utilitaires de test de Spring Security. Sans elle, la tranche
             @WebMvcTest monte sans filtre de securite et tout endpoint
             repondrait 200 : les tests de 403 passeraient a cote de ce qu'ils
             pretendent verifier. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Jar executable (Sprint 8.2). Declare module par module et non
                 dans le parent : rations-audit-commun est une bibliotheque, et
                 repackagee elle deviendrait inutilisable comme dependance.
                 L'execution "repackage" vient du parent Spring Boot. -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>
