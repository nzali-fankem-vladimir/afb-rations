# Aucune route configuree : le routage vers les services est defini au Sprint 8.
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka}

logging:
  level:
    root: INFO
    cm.afrilandfirstbank.rations.gateway: DEBUG
